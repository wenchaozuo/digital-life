{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat-window",
  "description": "Capability for the chat window",
  "windows": ["chat"],
  "permissions": ["core:default", "chat-commands"]
}
