[package]
name = "digital-life-vita-agent-protocol"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "vita_agent_protocol"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
