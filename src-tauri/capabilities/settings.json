{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Capability for the storage settings window",
  "windows": ["settings"],
  "permissions": ["core:default", "dialog:default"]
}
