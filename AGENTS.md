# AGENTS.md

## 项目定位

- 本项目是面向个人、运行于 Windows 本地的数字生命桌面工具。
- 它不是 SaaS、多用户/多租户平台，也不是普通聊天桌宠。
- 模型、Agent、工具和插件是能力来源，不是数字生命本体。
- Windows 10/11 是当前唯一需要正式支持的平台。

## 技术栈

- Tauri 2、Rust、Vue 3、TypeScript、Pinia、SQLite、LanceDB、PixiJS、Live2D。

## 核心架构边界

- SQLite 是权威数据源；LanceDB 是可从 SQLite 重建的向量索引。
- Agent、插件不得直接修改核心人格、长期记忆、关系状态或系统权限，只能提交候选变更供核心审核。
- 所有文件、命令、浏览器和系统操作必须经过 Tool Broker。
- LiteLLM 不属于首版硬依赖。

## 安全与任务约束

- API Key 不得写入普通配置、日志、测试数据或备份；应使用 Windows Credential Manager。
- 所有长任务必须支持 `request_id`/`task_id`、取消、超时、进度和明确最终状态。

## 开发规则

- 每次只完成当前任务，不擅自扩大需求，不重构无关模块，不新增未经批准的依赖。
- 不修改需求基线文档。
- 修改后运行相关测试或编译检查，并输出修改文件、结果和遗留问题。
- 详细设计中的代码、DDL、目录是契约或建议，不代表提前实现未来功能。

## 版本顺序

V0.1 基础生命体 → V0.2 记忆与内在状态 → V0.3 语音视觉与角色兼容 → V0.4 Agent 执行 → V0.5 多 Agent 与技能 → V0.6 插件与多形态 → V1.0 稳定整合。

## 需求依据

- 统一需求基线优先于详细设计及其他说明文档。
- 若文档与任务指令冲突，停止扩大实现并报告，等待确认。
